<?xml version="1.0" encoding="utf-8"?>
<!--
  The date of one month cell. Split from the cell's event stack because the
  all-day band sits between them: a bar spanning five days has to be a child
  of the week row, not of any single day.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/day_root"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1">

    <FrameLayout
        android:layout_width="@dimen/widget_month_day_marker"
        android:layout_height="@dimen/widget_month_day_marker"
        android:layout_gravity="center_horizontal">

        <ImageView
            android:id="@+id/day_marker"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:src="@drawable/widget_shape_circle"
            android:visibility="gone" />

        <TextView
            android:id="@+id/day_number"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textSize="@dimen/widget_month_day_number_size" />
    </FrameLayout>
</FrameLayout>
