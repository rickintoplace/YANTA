<?xml version="1.0" encoding="utf-8"?>
<!--
  Dense variant of widget_quick_action: same anatomy, smaller target, no
  label. Used when many actions share a narrow widget, where the regular
  40dp circle would be clipped rather than merely tight.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/action_root"
    android:layout_width="0dp"
    android:layout_height="match_parent"
    android:layout_weight="1">

    <FrameLayout
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="center">

        <ImageView
            android:id="@+id/action_background"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:src="@drawable/widget_shape_circle" />

        <ImageView
            android:id="@+id/action_icon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:padding="7dp" />
    </FrameLayout>
</FrameLayout>
